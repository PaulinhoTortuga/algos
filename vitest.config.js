module.exports = {
  // The entry file for your tests
  files: ["tests/**/*.test.js"], // Adjust this to match your test file pattern

  // Configure the browsers you want to test in
  browsers: {
    chrome: true, // You can add more browsers here if needed
  },

  // Additional configurations can be added as per your requirements
  // ...
};
